export default function Header() {
  return (
    <header className="bg-white p-4 flex items-center justify-end shadow-sm border-b border-primary-border">
      {/* Future controls (profile, settings) go here */}
    </header>
  );
}
//this just makes an top bar on the top casting some type of shadow 